// Tailwind CSS v4 dùng plugin PostCSS riêng (@tailwindcss/postcss).
// Đặt ở gốc repo. globals.css chỉ cần: @import "tailwindcss";
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
